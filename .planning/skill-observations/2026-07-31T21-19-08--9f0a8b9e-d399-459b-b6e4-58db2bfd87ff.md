## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Make `change-gate-conformance.sh` fail (non-zero, explicit) when the gate file it is asked to certify is missing or empty, instead of reporting `SKIP (not found) / TOTAL: 0 passed, 0 failed` and exiting 0.
Task size: **medium** — it changes the harness's exit-code contract, which downstream host CIs depend on.

Skills I will invoke, in order:
1. `superpowers:systematic-debugging` — this is a bug; Observe → Hypothesize → Test → Conclude before proposing a fix. Establishes whether the false-green is in the `--family` sweep, the single-file path, or both.
2. `/opsx:propose` — the harness's exit-code behaviour is a promise core makes to every host CI; changing it is a spec delta, not an incidental edit. (If the debug phase shows the current behaviour already contradicts a written spec, I'll say so and downgrade to a straight bugfix with no delta.)
3. `openspec validate --all` + `run-plan-review.sh` — Stage 2, before any code. Validate is enforced; I'll seek ≥2 other-vendor reviewers on the delta.
4. `superpowers:test-driven-development` — a `test(RED)` commit proving the harness exits 0 on a missing gate, then `feat(GREEN)`.
5. `superpowers:verification-before-completion` — before claiming done.
6. `superpowers:requesting-code-review` — independent Stage-2 review; validate does not discharge it.
7. `superpowers:finishing-a-development-branch` — branch + PR (never commit to main).

Post-phase gates: `/review` before landing. No `/cso` — no auth, storage, secrets, or LLM trust boundary touched. No `/qa` — no user-visible surface, no dev server.

Verification evidence I will produce: the RED test output showing exit 0 on a missing gate file; the GREEN output showing non-zero; `openspec validate --all` green; `REVIEWS.md` reviewer count; the harness re-run against opencode's `--family` sweep proving it no longer scores a missing host as a pass.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

---

Starting with systematic debugging — observing the actual behaviour before hypothesizing.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/systematic-debugging

# Systematic Debugging
