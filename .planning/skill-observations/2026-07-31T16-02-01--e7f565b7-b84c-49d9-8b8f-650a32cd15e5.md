## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Implement `track-and-conform-plan-review` — track the producer in core, conform the reviewer floor, add verdict/substance/identity/digest enforcement across spec §18, producer 1.1.0, gate 1.5.0, and reviewer-cli 1.2.0.
Task size: large

Skills I will invoke, in order:
1. `superpowers:test-driven-development` — the task list marks ~40 tasks `tdd="true"`; each needs an observed RED before GREEN
2. `superpowers:verification-before-completion` — before every task-completion claim; evidence, not assertion
3. `superpowers:systematic-debugging` — if any mid-change bug appears
4. `superpowers:requesting-code-review` — Stage-2 independent review after implementation; `openspec validate` does not discharge it
5. `superpowers:finishing-a-development-branch` — compose the PR when the branch is ready

Post-phase gates (if applicable): `review` (Stage-2 independent code review). No `cso` — this change touches no auth/storage/request handling; it does touch a trust boundary (reviewer output), which the change already specifies and which I will flag if implementation widens it. No `qa` — no user-visible UI surface.

Verification evidence I will produce: `test(RED)`/`feat(GREEN)` commit pairs per TDD task; `tools/change-gate-conformance.sh` green with zero inconclusive; `openspec validate --all` green; the §18 gate green via `--ci`; and a re-review of this branch under gate 1.5.0 proving trailered evidence reads current and pre-trailer evidence reads unverifiable (task 9b.19).

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

Let me confirm the gate actually lets me edit code, and read the full task list.

Gate is green (exit 0 — rejections are NOTEs, allowed on quorum). Let me explore the repo conventions before writing anything.

I have the conventions. This is ~130 tasks; let me track progress explicitly.

Starting §2 — seed the tracked source byte-identically, so the baseline is provably the running implementation:

Byte-identical, 227 lines, marker 1.0.0 — matches the task's stated baseline. Now the README; let me match a sibling's conventions:

Now writing the producer README in the same shape:

Layout matches siblings, executable bit preserved, ancestor is 66 lines (so my "161 lines divergence" claim is right). Registering it in the index:

§2 complete. Committing, then marking tasks 2.1–2.4 done:

Now the producer test harness — the TDD tasks need a test surface, and none exists. Let me read the full producer first:

Now I understand the producer. Note several defects the tasks target: line 212 defaults `MIN=2`, line 214's guard `''|*[!0-9]*` **accepts `0`**, line 126 silently skips self rather than recording exclusion, and line 101 globs `specs/*/spec.md`.

Building the harness infrastructure plus the §3 floor rows first:

Running RED — this must fail before I touch the producer:

Exit 127 across the board — that's my harness bug, not a genuine RED: `run_row` does `cd "$fx/repo"` while `$PRODUCER` is a relative path. Fixing so the failures are real:

Genuine RED, and it confirms both defects precisely. Let me check the sharpest form of the `MIN_REVIEWERS=0` hole before fixing:

Confirmed, and it's worse than the change describes: `MIN_REVIEWERS=0` **overwrites `REVIEWS.md` with a zero-byte file and exits 0** — destroying prior evidence and publishing empty artifact. Adding that row, then implementing:

Committing the RED harness before implementing, per TDD discipline:

RED confirmed — 5 failures, including the zero-byte evidence destruction. Now GREEN:

One more issue: the floor is validated *after* the reviewer loop, so a malformed invocation still ships the change to third parties before erroring. Moving validation ahead of egress:

GREEN — 10/10. Committing the pair:

§3 done. Now §4 — verdict-and-substance counting. Adding the RED rows:

Genuine RED — 6 failures, including both production regressions. Now the shared verdict-and-substance predicate:

Now wiring it into the loop, alongside the trailer guard:

22/22 GREEN. Committing:

Now §5 — the implementing-host identity. Let me check what interface the spec delta requires before writing rows:

The spec requires "explicit input" without mandating the channel. I'll support a flag and keep `AGENT_SELF` honoured — but with **no default**, which is the breaking change §5.8 migrates. Adding RED rows:

A measurement tool must not inherit state from what it measures — the sibling harness unsets `OPENSPEC_GATE_SELF` for exactly this reason. Applying the same rule:

Clean RED — 10 identity failures. Implementing:

Now the validation and exclusion logic:

Now the loop: record exclusions rather than silently skipping, and deduplicate vendors:

Now recording failures and writing the record block plus trailer — the identity rows need both:

Now the record block and trailer at publish time:

Now the digest and the publish block. Let me read the digest contract to implement it exactly:

The digest contract is fully specified. Implementing it as a shared function:

Now widening the prompt glob to match the digest set (task 7.2 — one set, stated once) and capturing one snapshot:

Now the publish block — record, notice, trailer, and the pre-publication drift check:

Two things: the older rows now correctly fail because identity is required (that's the breaking change working), and my dedupe row's expectation was wrong — one counted reviewer meets a floor of 1. Fixing both:

The rows wrap across lines, so my regex missed the argument line. Direct fix:

My blanket substitution over-applied — it added an identity to the three rows that must *not* have one. Reverting those:

33/33 GREEN. Let me inspect a real produced artifact and verify the digest independently (task 7.10):

The artifact is well-formed. Now task 7.10 — computing the digest independently, by hand, to prove the contract is reproducible:

Task 7.10 verified — an independent implementation written straight from the spec text produces the identical digest. Adding the digest and record rows:

Adding the three helpers those rows need:

All 48 pass — but these last rows passed on first run, because I wrote the digest/record/trailer code before the rows. That's a TDD violation (no RED observed). Let me verify the rows actually discriminate by scoring the untouched 1.0.0 baseline:

Every new row fails against 1.0.0 — they discriminate, so the RED evidence is genuine even though obtained retrospectively. Adding rows for the two remaining digest properties (7.4, 7.6):

That's a real defect in my implementation, not a bad row. Let me diagnose:

Two findings. The `assert_ne` message misled me — it prints `$2` for both. And the producer *does* fail closed on a newline path (exit 2), but with a wrong reason: the path was split, not a symlink. The spec's framing rationale explicitly contemplates digesting such paths, so let me make enumeration NUL-safe:

50/50 GREEN, including the newline-path row. Committing this batch:

Now the invocation-time egress notice (9d.7) and the remaining §6 verifications:

53/53. Now §8 — bump the marker and publish. First confirming the sanitiser and forge guard are unmodified (task 8.1):

My sed range was too greedy. Comparing just the awk sanitiser program precisely:

The forge guard is identical and the 3/4/5 messages are unchanged in text (only `;;` moved because I added the recording line). Let me extract the awk sanitiser by line number to settle it:

Task 8.1 verified: sanitiser awk program, forge guard, and 3/4/5 messages all unmodified. Now bumping the marker and updating the stale header:

Marker at 1.1.0, 53/53 green. Now task 5.8 — the caller inventory, which is the migration for this breaking change:

Most are documentation mentions. Let me separate actual *invocations* — those are what need migrating:

**Key finding: there are no executable callers.** Every hit is documentation — diagram lines, descriptive JSON strings, install echoes. Nothing scripts the producer, so nothing silently breaks. Recording this as evidence:

Task 5.9 verified — names the missing input, never guesses. Now updating core's documented usage and committing:

Now §8.3–8.6. Let me look at the artifact resolver:

Both siblings still green. Now publishing (8.5) and reproducing the 2026-07-29 failure end-to-end (8.6):

Published, and the arbiter correctly refuses the downgrade — which is exactly why §10.3's opt-in flag is needed. Now 8.6, reproducing the recorded failure:

8.6 verified end-to-end against the published 1.1.0 — the recorded failure now yields written, trailered evidence. Let me commit, then start the §8b re-review in the background while I build the gate:

§8b is running (~10 min). Meanwhile I'll build the gate — being careful not to touch those changes' artifacts while the review is in flight, or the mutation check will refuse to publish.

Now the gate. The two functions deliberately duplicate parsing; task 9b.3 requires one shared predicate. I'll add a single-pass parser both consume:

Now replacing the two counting functions to consume that single parse:

My replacement cut before `pending_rejections`'s closing brace, leaving an orphan. Fixing:

36/52. The 16 failures are old rows seeding **untrailered** evidence — which now correctly counts zero. That's the migration consequence, applied to the harness. 

One correction first: the harness *does* score 52 rows (computed, not declared), so "52-case harness" was accurate-but-brittle rather than fabricated. I'll fix that note once §8b finishes writing.

Let me migrate the harness fixtures:

Still 16. Let me debug a fixture directly rather than guess:

The interleaved output is just deferred flush — not a bug. Let me source the harness's own helpers and compare its digest against the gate's:

`make_fixture` needs its validate-exit argument — my call omitted it. Re-running properly:

Found a real defect I introduced. The harness seeds `implementing-host: pi` — and **`pi` is one of the four hosts** but not a reviewer vendor. My closed vocabulary conflates two different sets:

Confirmed — two distinct sets that my spec text conflated:

- **Hosts** (author changes): claude, codex, opencode, **pi**
- **Reviewer vendors** (have `reviewer-cli` arms): claude, **gemini**, opencode, codex

`pi` is a host with no reviewer arm. My vocabulary excluded it, so a **pi-authored change could never declare its identity** — producer usage error, and every pi artifact reading `trailer-bad-host` → zero reviewers → permanently blocked. Fixing both implementations to the union:

47/52 — that fix recovered 11 rows. Let me see the remaining 5:

Row 543 expects exit 0 from two *verdictless* reviewers — 1.4.0 semantics, where a heading counted. Under 1.5.0 that's correctly zero. Let me read all five and update them to preserve intent under the new rule:

All four hand-built fixtures lack trailers, and several have verdict-only sections with no body — both now correctly count zero. Updating them to 1.5.0 semantics while preserving each row's intent:

52/52 green. Let me check on §8b and commit this progress:

I've hit a natural checkpoint. Let me report where things stand while the re-review finishes.

**Done so far** (8 commits on `feat/step3-hook-shims-and-dead-gate-removal`):

- **§2** — producer tracked in core, seeded byte-identically from the running copy
- **§3–§7b** — producer 1.0.0 → **1.1.0**, published; 53/53 on a new conformance harness
- **§8** — resolver mapping, published, arbiter verified, 2026-07-29 failure reproduced end-to-end
- **§9b** (partial) — gate counting rewritten to one shared parse; 52/52

**Three real defects found by building it, not by reading it:**

1. **`MIN_REVIEWERS=0` destroyed evidence.** The old guard rejected non-integers and negatives but passed `0`. With every reviewer failing, `0 -lt 0` is false — so the producer overwrote `REVIEWS.md` with a **zero-byte file**, reported "wrote 0 reviewer section(s)" and exited 0. Verified against the installed binary before fixing.

2. **Newline-bearing paths silently left the digest set.** I specified length-framing precisely so a path containing a newline couldn't forge a record boundary — then enumerated the set line-by-line, which splits such a path into two nonexistent ones. Framing boundaries is pointless if the boundary is lost on the way in. Now NUL-delimited end to end.

3. **My own spec text would have locked `pi` out of the fleet.** This is the serious one. The change specifies the implementing-host vocabulary as the *reviewer* set — but hosts and reviewer vendors are different sets: `pi` is a host with no reviewer arm, `gemini` is a reviewer that is no host. A pi-authored change could never declare its identity: producer refuses it, gate reads any hand-written trailer as malformed, zero reviewers, permanently blocked. **Six review rounds and three vendors did not catch it**; the harness did, because it seeds `implementing-host: pi`. Both implementations now take the union.

That last one also corrects something I told you earlier: I reported the "52-case harness" as fabricated. The harness genuinely scores 52 rows — it computes the total rather than declaring it, so the number was accurate but brittle. Dropping it was still right (I'm about to add rows), but "fabricated" overstated it.

**Still to do:** §1 (spec §18 edits), the spec-delta correction for the `pi` defect, §9b completion (remaining TDD rows, publish 1.5.0), §9c wrapper, §9 ancestor deletion, §10 downgrade flag + ADR + Stage-2 review.

The §8b re-review of both changes is still running against gemini/codex/opencode. Once it lands I'll fix the spec delta for the `pi` defect — that correction needs a fresh review afterward, since amending an artifact stales its evidence under the very rule being built.

Both re-reviewed with 1.1.0 trailers. Note opencode produced **no output at all** on `shim` — the fourth time (rounds 2, 4, 6, now). Let me verify the evidence and read the new reviews:

Both read `ok` under the new gate — 8b.5 verified. Let me read what round 7 found:

Round 7 is sharp. Let me verify the load-bearing claims before acting:

Both confirmed real. Now the functional one — whether a reviewer quoting the trailer **inside a fenced block** gets rejected:

Confirmed — a **real functional bug in my code**. A reviewer quoting the grammar inside a fence (the natural way to quote a multi-line block) has its whole review rejected. That's the exact failure I wrote paragraphs warning about, shipped anyway because I anchored at line start without fence awareness. Fixing both guards:

Now opencode's item 5 — whether producer-appended content can manufacture "substance" and reopen the 2026-07-29 hole. Testing my implementation directly:

`SECTION gemini APPROVE 0` — substance stays 0, so the bare-verdict hole remains closed in my implementation. opencode's item 5 is a genuine **spec** gap (placement unspecified) rather than a code defect.

§8b has finished, so I can now correct the artifacts. Batching all round-7 fixes:

Now the placement/substance gap (opencode #5, codex overlap) and the fence-aware guard definition:

Now the `pi` vocabulary defect and codex's remaining points:

Now codex's two substantive remaining points — the discarded-reviews goal and downgrade-log failure semantics:

The harness reports 55, not the 56 I wrote in the commit message. Amending — a wrong test count in the commit that fixes evidence-integrity bugs is the wrong place to be sloppy:

Now §1 — repairing spec §18 itself, which must precede the gate blocking on verdicts:

The two surviving §18 mentions are the historical rationale ("why the floor moved from two to one") — correctly left. Now task 1.8's checklist, distinguishing floor statements from preference and unrelated subjects:

Every survivor is legitimate: design *alternatives* (different subject), the independence-preference prose, and the gate's explicit `MUST >= 1, SHOULD >= 2`. Now the version bump and changelog:

Now finishing §9b. Task 9b.19 requires testing **both** directions — trailered evidence reads current, pre-trailer evidence reads unverifiable:

All three directions behave correctly. Note `track-and-conform-plan-review` reads **`digest-mismatch`** — because I amended its artifacts during the round-7 corrections after the re-review. That's the mechanism correctly detecting my own amendment, on the change that builds it.

Bumping the gate marker (publishing comes after re-review, per the migration order):

Now §9c — the wrapper's process-table exposure. Let me see how it currently passes prompts:

All four arms put the **entire change** in argv — visible in `ps` to any local user. Let me check what delivery each CLI actually supports before changing them:

`codex exec` explicitly reads stdin when the prompt isn't an argument — so the original "hangs on stdin" friction was an empty TTY, not an inability to read. Let me confirm the other three:

All four support non-argv delivery. Let me read the wrapper's bounding function before changing the arms:

`argv-leak=0` everywhere (my probe's output formatting is mangled — `grep -c` plus the `|| echo 0` fallback). But 3 conformance failures need attention:

Those rows assert stdin is pinned to `/dev/null` — the *old* contract. The property they protect (the vendor never blocks on stdin) still holds, but the mechanism changed. Let me read them:

The row's real purpose — the caller's ambient stdin must not reach the vendor — still matters; only the delivery changed. Rewriting it to assert that precisely:

The stub needs to record its argv for the new row. Let me wire that:

19/19, up from 15 rows. Now 9c.3 — a real smoke test that `codex exec` still works via stdin (the arm that carried the hang):

`codex exec` works via stdin — no hang, exit 0. Let me verify the other three arms too, then publish:

`claude` works. Let me check `gemini` and `opencode` more carefully — their output was truncated by my `tail -2`:

Smoke-testing found two real failures the stub harness cannot see: **gemini does not consume piped stdin** ("I cannot directly read from `stdin`"), and **opencode exits 1 with no output**. Let me find what each actually accepts:

Two findings: **gemini `@file` works** (returns ALIVE), and **opencode's `-f` is an array option** that greedily swallowed my message as a second filename. Let me find the right opencode form:

**opencode reads stdin correctly.** So the empirically-verified delivery per arm is: `claude` stdin, `codex` stdin, `opencode` stdin, `gemini` `@file` path. Correcting the wrapper:

Critical: **gemini's `@file` only reads paths inside the workspace** — my earlier test passed only because I ran it from `/tmp`. The producer writes prompts to `$TMPDIR`, so this would always fail in real use. Let me check whether gemini stdin genuinely fails:

**gemini does read stdin** — my earlier failure was the *hint wording*, not a capability limit. Telling an agentic CLI the content is "on stdin" made it try to go read a file called stdin. Phrased as "above", it works. All four arms can use stdin uniformly:

All four arms verified against real vendors, 19/19 harness. Publishing the wrapper and committing:

`gate/` is untracked, so deleting is irreversible. The change plans this (task 9.2), but let me look at the target and preserve it first:

Now §10 — the downgrade flag, which every rollback row depends on:

Writing the RED rows for the downgrade flag first this time:

Genuine RED on the three substantive rows. (The four passing rows can't yet distinguish "correctly refused" from "refuses everything" — they only become meaningful once the flag exists.) Implementing:

20/20. Now 10.4 — rehearsing the actual rollback the migration plan promises, rather than asserting it:

The rollback path is executable end to end, with a proper tab-separated audit record. Committing §9, §9c and §10's flag:

Now the final sequence. Let me re-review `track` (its digest went stale when I applied the round-7 corrections) while I finish the gate rows and ADR:

Let me verify the specific 9b rows are genuinely covered rather than assumed, by adding the ones the 52-row harness doesn't yet pin:

Several 9b properties are implemented but untested at the gate level. Adding a section for them:

66/66. Let me confirm these new rows discriminate rather than being vacuous, by scoring the old 1.4.0 gate:

10 of 14 fail against 1.4.0. The 4 that pass on both are "allow" rows — 1.4.0 satisfied those too by ignoring trailers entirely, so they guard the permissive direction rather than being vacuous.

Let me check the re-review and write the ADR:

Still running. Let me report progress while it finishes.

**Implementation is essentially complete.** 12 commits on `feat/step3-hook-shims-and-dead-gate-removal`:

| Artifact | Was | Now | Harness |
|---|---|---|---|
| `spec/18` + `spec_version` | 1.2.0, self-contradictory | **1.3.0**, counting terms added | — |
| `run-plan-review.sh` | 1.0.0, untracked | **1.1.0**, tracked + published | 55/55 |
| `openspec-change-gate.sh` | 1.4.0 | **1.5.0** (not yet published) | 66/66 |
| `reviewer-cli.sh` | 1.1.0 | **1.2.0**, published | 19/19 |
| `install-shared-artifact.sh` | no rollback path | `--allow-downgrade` | 20/20 |

**Five defects found by building rather than reviewing** — all verified against the running code before fixing:

1. **`MIN_REVIEWERS=0` destroyed evidence** — published a zero-byte `REVIEWS.md` over good evidence and exited 0.
2. **Line-based digest enumeration dropped newline-bearing paths** — I specified length-framing to stop a path forging a record boundary, then lost the boundary during enumeration.
3. **My spec text would have locked `pi` out of the fleet** — hosts and reviewer vendors are different sets; validating the implementing host against the *reviewer* set makes every pi-authored change permanently unreviewable. Six review rounds across three vendors missed it.
4. **Fence-blind guards rejected engaged reviews** — quoting the trailer grammar in a fenced block (the natural way) got the whole review rejected. Exactly the failure round 6 established the principle for; I fixed the inline case and stopped.
5. **Stub harnesses can't verify vendor integration** — the stdin conversion scored green on all four stubbed arms while two were broken against real CLIs. `opencode`'s `--file` is an array option that ate the message; `gemini` refused because the *hint wording* told an agentic CLI to go open something called "stdin".

Round 7 (the first digest-bound review) also caught two genuine self-contradictions I'd shipped: digest framing stated two ways across `design.md` and the delta, and a rejected glob variant surviving in a normative scenario.

**Deliberately not done yet:** publishing gate 1.5.0. Publishing it before the re-review lands would block every change in this repo — including my own edits — since `track`'s evidence went stale when I applied the round-7 corrections. That ordering is the migration plan's central claim, so I'm following it rather than working around it.

Remaining: publish the gate, run `--ci`, and the Stage-2 independent code review (§07 requires it in a separate context; `openspec validate` doesn't discharge it).

Round 8: both counted reviewers REQUEST-CHANGES, opencode timed out at 600s (recorded, not silently dropped). Gate reads the evidence as `ok`. Let me see what they found:

Let me verify these before acting:

Verified: **the vocabulary contradiction is real** (proposal, design, and task 5.2 still say four while the delta says five — I fixed only the deltas). **The substance bypass is not** — `SECTION gemini APPROVE 0`, headings already excluded. And codex's "four known vendors" scenario doesn't exist.

Let me check the inventory over-claim:

Confirmed — my own inventory claims corrections I never made. Fixing both that and the vocabulary contradiction:

Now publishing gate 1.5.0 — the last step of the migration order — and verifying:

Gate 1.5.0 is live and **correctly blocking** — `track` reads 0/1 reviewers because I amended it after round 8. The mechanism is now gating its own change. Let me commit (OpenSpec artifacts are exempt) and run the final re-review:

Writing the handoff while the final review runs:

Still running. The implementation is complete; here's where it stands.
