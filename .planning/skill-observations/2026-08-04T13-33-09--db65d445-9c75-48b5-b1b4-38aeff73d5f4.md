## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Propose a new OpenSpec change for **the instrument change** — make `project-hook-conformance.sh` report per-project checkout state under `--fleet`, declare core's two non-bindings in `OPT-OUTS`, and stop counting archived change docs as override vectors.
Task size: **Medium** (multi-file shell + spec delta; opens an OpenSpec change)

Skills I will invoke, in order:
1. `superpowers:brainstorming` — the change adds new behavior to an existing instrument, and item 3 carries a genuinely open design question (does the scan distinguish *names* the variable from *sets* it?). ≥2 alternatives land in `design.md`.
2. `/opsx:propose` — author proposal + design note + spec delta + tasks for the new change.
3. `openspec validate --all` — Stage 2, enforced, before any code.
4. `run-plan-review.sh <slug>` — multi-AI change review before code exists; two independent other-vendor reviewers preferred, one is the floor.

Post-phase gates (if applicable): Stage-2 independent code review at execute time (a cleared session, never a subagent, per §07). No UI surface, no auth/storage/schema — `/cso`, `/qa` and `database-sentinel` do not trigger.
Verification evidence I will produce: `openspec validate --all` green; `REVIEWS.md` with reviewer count; the three items each stated as a spec requirement with a scenario; re-run of the instrument's current numbers so the delta's before/after claims are measured, not remembered.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

Now brainstorming — starting with the actual current behavior of the instrument rather than the handoff's summary of it.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
