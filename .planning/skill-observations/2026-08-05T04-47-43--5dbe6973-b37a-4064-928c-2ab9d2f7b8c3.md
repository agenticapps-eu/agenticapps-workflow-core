## Workflow commitment

I am using the agentic-apps-workflow skill for this task.

**Task scope:** Revise core spec §08 to a machine-executable migration format, build the `@agenticapps/workflow` npx installer that replaces `install.sh` across four hosts, and prove it with a one-off Docker smoke run.

**Task size:** large — and larger than one change. This is a 7-phase program spanning core plus four host repos plus a published npm package.

Skills I will invoke, in order:

1. `superpowers:brainstorming` — the brief ends with four explicitly unresolved questions ("Surface, do not guess"); ≥2 alternatives per question land in `design.md`.
2. `/opsx:propose` — Phase 1 (spec §08 revision) as the first OpenSpec change. Core-only, independently revertible, and the contract every later phase depends on.
3. `openspec validate --all` + `run-plan-review.sh <slug>` — Stage 2, before any code. Validate is enforced; two other-vendor reviewers are the discipline I keep myself.
4. `superpowers:test-driven-development` — the extractor, runner, and format linter are pure text-in/tree-out. RED commit before GREEN, no exceptions.
5. `superpowers:systematic-debugging` — reserved for the phase-2 differential: if agent-applied and runner-applied trees differ, that diff is the thesis under test, not a bug to paper over.
6. `superpowers:verification-before-completion` — before every task completion.
7. `superpowers:requesting-code-review` — Stage 2, independent cleared session (per my standing memory on §07 independence).
8. `superpowers:finishing-a-development-branch` — the PR, as an act separate from archiving.

**Post-phase gates:** `/cso` is **not** conditional-away here and I flag it now — the installer writes to a shared `~/.agenticapps/bin` consumed by every agent on the machine, executes shell from markdown fences, and the smoke run handles long-lived API tokens. That is a trust boundary. SECURITY.md when the CLI phase opens.

**Verification evidence I will produce:** re-derived fence/quartet counts from the live repos (replacing the brief's 2026-08-03 figures); a byte-identity diff of the 16 codex migrations applied both ways; a format-linter RED test; and a smoke-run transcript including the deliberately-neutered-gate check going red.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

---

Before proposing anything, the brief instructs that every count in Part 1 be re-derived. Doing that first — a proposal built on stale numbers is worse than no proposal.

Fence counts confirm the brief exactly: **91 `bash`, 1 `yaml`**. Now the quartet discipline claim.

16 migrations (0000–0015), as stated. Now the structured-section discipline per migration — the claim that 13 of 16 carry the full quartet and that 0014/0015 have none.

One correction to the brief, and it matters. Let me confirm the shape before I write it down.

Confirmed with one substantive correction. Now the remaining Part 1 claims — 0000's placeholder count, and the 0009/0012 audit targets.

Two findings that change the shape of the work. Let me pin them down.

An extractor already exists — and it's keyed on prose labels, not fence info-strings. That's a direct fork with the brief's design. Let me check how far it reaches before I raise it.

§08 already mandates the quartet as a MUST. That reframes things — 0014 and 0015 aren't awaiting a new format, they're non-conformant against the current one. Let me size the fleet-wide picture before I bring this to you.

Fleet-wide that's **73 migrations, 259 steps, 445 bash fences, 1,544 lines of `install.sh`** — the retrofit is ~4.5× the codex-only scope the brief costs out. But whether that retrofit is needed at all depends on one thing: which migrations ever need to *execute* again. Let me check.

The 7 consumer projects in FLEET are what `workflow update` actually runs against. Their installed versions determine which migrations must ever execute again.

I've re-derived Part 1's numbers and found four things that change the plan. Reporting before I propose anything.

**Confirmed exactly as written:** 91 `bash` fences + 1 `yaml`; 16 migrations; 13 of 16 carry the complete quartet on every step; `0014` has 7 steps and zero structured sections; `0000`'s interactivity is a form, not judgement.

**Four corrections:**

1. **`0015` is partial, not "zero structured sections."** It has 5 steps: step 1 has the full set, step 2 has an idempotency check but no pre-condition, steps 3–5 have nothing, and *no step has a rollback*. That's worse than zero — a runner keyed on "does this migration have structure?" sees structure and under-applies silently. (`0006` also has a one-step gap: 3 steps, 2 pre-conditions.)

2. **§08 already mandates the quartet as a MUST** — "Every step MUST have four sections, in this order" (`spec/08-migration-format.md:81`). So `0014`/`0015` aren't decay awaiting a new format; they are *already non-conformant* against spec 0.9.1. The format linter in Part 1.3 needs no spec revision to justify it — it enforces an existing MUST, and could ship before Phase 1 rather than after Phase 3.

3. **A hardened fence extractor already exists.** `codex-workflow/migrations/run-tests.sh` is **7,176 lines**, and lines 71–190 are `extract_step_block()` — fence-scoped extraction keyed on `**<Label>:**`, with a delimiter guard against `Step 1` matching `Step 10+` (added under review as D-12), an inline-code-span fallback for `0007`/`0008`'s single-line steps (MIGR-08), and a deliberate literal-prefix match so a metacharacter in a label can't inject a regex. It was ported from `claude-workflow` at a pinned SHA. The brief's `role=` info-string design would discard all of it and re-solve those two document shapes.

4. **`0000` has 14 distinct placeholders, not 12** — the 8 named and 4 bars, plus `{{BACKEND_OVERRIDE}}` (conditional on `BACKEND = Other`, so the answers schema needs conditionals, not just defaults) and `{{PLACEHOLDERS}}`, which is prose referring to the others rather than a real field.

The scope finding is the one that matters most:

**The scope finding:** the seven pending migrations for `cparx` contain *exactly* the four the brief flags as known work (`0009`, `0012`, `0014`, `0015`), plus three more. Everything else in the fleet is current, and the other three hosts have no project with anything pending.

So the population the runner must actually execute against is **7 migrations in one host for one project** — not the 445 bash fences across 73 migrations that a fleet-wide retrofit implies. That doesn't weaken the case for executable migrations (the "installer can't apply prose" argument is about *future* migrations and is untouched), but it does mean the retrofit cost is a choice rather than a given.

Two decisions I won't guess at:

What would you like to clarify before choosing?

To make that easier, here's what's behind each question and what I deliberately left out:

**On the fence contract** — the thing I can't resolve from the repos is *why* the brief chose `role=` when the label markers were already there and already extracted. Three possibilities, and they lead to different answers:

- If the reasoning was "prose labels are fragile," the hybrid gets you the robustness without discarding the extractor.
- If it was "I want one unambiguous contract in §08," then `role=` only is right and the extractor loss is an acceptable cost.
- If it was simply "I didn't know `extract_step_block()` existed" — which is plausible, since it lives 71 lines into a 7,176-line test harness — then the whole question is new information and the brief's position isn't really a position yet.

**On retrofit scope** — I framed it around what must *execute*, but there's a second axis I didn't put in the options: the format linter. Once it lands as blocking, it fails every migration missing a block, regardless of whether that migration will ever run again. So scope isn't purely "what replays" — it's also "what the linter is allowed to be red against." Options: linter blocking on new migrations only, blocking on the retrofitted set, or blocking fleet-wide with a grandfather list.

**What I have not investigated yet**, and could before you decide: `install.sh` itself (the 396 lines Part 2 must port), the three sibling hosts' migration structure beyond raw counts, the `agenticapps-shared` submodule that holds the shared harness primitives, and whether `claude-workflow`'s 34 migrations follow the same `**Label:**` convention or a different one. Any of those could change the picture again — the extractor finding did.

Is your question about one of these, about something in the brief I've mischaracterized, or about the scope of the session itself?

You're right — that was internal shorthand, not a question. Let me put it plainly.

**The situation.** Migrations are markdown files that mix explanation with shell commands. Today an AI agent reads one and does what it says. The brief wants a plain script to run them instead, so that installing the workflow doesn't require an AI with an API key.

For a script to do that, it needs to know *which* code blocks to actually run — as opposed to the ones that are just examples in the explanation.

**The brief's answer:** tag every runnable block, like ```bash role=apply.

**What I found:** there's already an answer in place. The migrations have bold headings — **Apply:**, **Rollback:**, and so on — and there's already working, tested code in the codex repo that finds the code block under each heading. It's been through review twice and handles two awkward real-world cases.

**So the actual question is:** add the new tags, keep the existing headings, or use both?

**My recommendation: both.** Keep the headings as they are — they're what makes the files readable, they're already required by the spec, and the existing code already reads them. Add the `role=` tag purely to mark "this block runs." That gets the one thing the headings genuinely can't do — letting a migration include an example code block without the script trying to execute it — without throwing away working code. The cost is that a step then has two markers that could disagree, so the linter has to check they match.

**The second question is just how much old work to go back and update.** There are 73 migration files across the four repos. But only one project anywhere (`cparx`) is behind and needs to replay anything, and it needs 7 files. The rest have been applied everywhere already and will never run again.

**My recommendation: those 7.** They happen to include all four files the brief already singled out as needing attention. Updating the other 66 is real work with no one to benefit from it.

---

If that sounds right, say so and I'll write it up as the OpenSpec proposal. If you'd rather I just follow the brief exactly as written — tags only, and retrofit all 16 codex files — that's a completely reasonable call too, and the only thing it really costs is the existing extractor plus more churn.

And if the useful thing right now is for me to go read `install.sh` and the other three repos first so the picture is complete before you decide anything, I'll do that instead.

That's a much better plan, and it collapses a whole category of work. Let me make sure I've got the consequence right:

Once `cparx` is at 1.2.0, **no project anywhere has a pending migration.** Fresh setup already uses the snapshot rather than replay (ADR-0036), so the installer never replays history. That means:

- **Retrofit scope becomes zero.** All 73 existing migrations stay exactly as they are, permanently. They're history, not code.
- **The executable format applies only to migrations written from now on** — so it's a greenfield contract, not an extraction exercise. No `apply-agent` escape hatch needed on day one either, since there's no legacy prose to accommodate.
- **The linter starts clean and blocking**, with no grandfather list, because it only ever sees new-format files. `0014`/`0015`'s missing rollbacks stop being a defect to fix and become a fact about a file that will never run again.
- **Part 2 shrinks too.** `workflow update` has no historic burden — it applies pending new-format migrations, of which there are currently none.

And on the format: **both**, confirmed. Headings stay as the readable skeleton, `role=` marks what executes, linter asserts they agree.

One consequence worth naming now: the smoke run's update leg (checkout N−1 → setup → checkout N → update) currently has nothing to test against, because every migration that exists is historic. It'll need at least one purpose-built new-format migration to exercise it. That's arguably better than testing against legacy files, but it's a thing to author rather than a thing to point at.

Let me verify the state of `cparx` before I plan the migration.

Two complications worth surfacing before I plan this.

A significant one. `cparx`'s two host installs are not in the same condition:

The picture changed again — `cparx` is primarily a **claude** project (`.claude/skills/agentic-apps-workflow`, live gate hook, updated 1 Aug), while `.codex/skills` and `.opencode/skills` are both empty. Let me find where claude-workflow records its version.

My earlier fleet scan was wrong, and the correction matters. claude-workflow doesn't use a `workflow-version.txt` — it stamps the version in `SKILL.md`. Let me redo it properly across all four hosts.

(The exit 1 is the loop's last test, not a failure — output is complete.)

**This changes the answer: there is nothing to migrate.**

Every repo in the fleet is current on its live host — all seven are on claude 3.2.0, and `agents-task-viewer`/`callbot`/`agenticapps-dashboard` are current on their second host too. The only two rows showing PENDING are `cparx`'s codex and opencode stamps, and both have **no skills installed**. They're dead version files describing host installs that no longer exist.

So `cparx` doesn't need a 7-migration replay. Let me confirm those two really are abandoned rather than just unusual.

Here's the resolved picture.

**`cparx` doesn't need migrating — the two PENDING rows are vestigial.** Its live install is claude 3.2.0, current, maintained. The codex surface (`.codex/`) was never committed to git at all and has no skills; the opencode surface has a tracked config but also no skills. Neither host can actually run the workflow there. The `0.5.0` stamps describe installs that were abandoned mid-setup around 20 July.

So your plan holds, and more cheaply than you proposed: **no migration run is needed at all.** The fleet is already at the state where historic migrations are dead. What's left is a tidy-up decision on two stale files in `cparx` — delete them, or leave them as archaeology. That's a small, separate call in another family's repo, and I'd rather not touch `factiv/` without you saying so explicitly. It doesn't block anything here.

**Settled, then:**

| | |
|---|---|
| Fence contract | **Both** — `**Label:**` headings stay as the readable skeleton, `role=` marks what executes, linter asserts they agree |
| Retrofit scope | **Zero.** All 73 existing migrations are frozen history |
| Format applies to | Migrations written from now on |
| Linter | Blocking from day one, no grandfather list |
| `apply-agent` | I'd **defer** it — the brief included it to absorb legacy prose, and there is no legacy prose in scope. Cheaper to add later than to remove a spec'd feature |
| Installer `update` | No historic burden; needs a purpose-built new-format migration to have anything to test against |

Starting the OpenSpec change for the §08 revision. Feature branch first, per the standing rule.

No existing capability covers migration format — this will add one. Invoking brainstorming as committed, focused on what's genuinely still open for this change.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
