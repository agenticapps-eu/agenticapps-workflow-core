## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Plan step 3 of `docs/PLAN-lightweight-fleet.md` — make projects carry almost nothing, so migrations become unnecessary.
Task size: large

Skills I will invoke, in order:
1. `superpowers:brainstorming` — step 3 is architecture design with a stated cost trade-off ("rules stop being repo-portable"); ≥2 alternatives must land in `design.md`
2. `/opsx:propose` — Large work opens an OpenSpec change: proposal + design note + spec delta + tasks
3. `openspec validate --all` — Stage 2 machine check, before any code
4. `run-plan-review.sh <slug>` — Stage 2 multi-AI review; ≥2 independent other-vendor reviewers into `REVIEWS.md` before code exists
5. `superpowers:test-driven-development` — for any task marked `tdd="true"`
6. `superpowers:verification-before-completion` — before every task completion claim
7. `superpowers:requesting-code-review` — Stage 2 code review, independent context; validate does not discharge it
8. `/opsx:archive` then `superpowers:finishing-a-development-branch` — archive and ship as two separate acts

Post-phase gates (if applicable): `review` — no UI surface, no auth/storage/schema/LLM boundary in scope, so `cso`, `qa` and `database-sentinel` do not trigger. I will re-evaluate if scope grows.
Verification evidence I will produce: `openspec validate --all` green; `REVIEWS.md` with ≥2 reviewer headers; RED/GREEN commit pairs for TDD tasks; `tasks.md` with zero unchecked items; `openspec-change-gate.sh --ci` green; an ADR recording the repo-portability trade-off.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

Let me check the current state of this repo's change slot and the concrete per-project payload before brainstorming.

callbot carries considerably more than the plan's four items — 8 hook scripts, several of them full implementations rather than shims. Let me measure the real per-project payload across the fleet before brainstorming.

The empirical case is stronger than the plan assumed. Key finding: **`openspec-change-gate.sh` is the only hook using the shared-bin shim pattern — and it's one of the few with zero drift. Four of the eight copied hooks have already silently diverged** (database-sentinel and normalize-claude-md into 3 versions each).

Now brainstorming, as committed.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
