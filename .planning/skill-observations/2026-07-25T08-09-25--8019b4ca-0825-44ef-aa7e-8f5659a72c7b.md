## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Land `gate/` as a tracked, canonical §18 change-gate in agenticapps-workflow-core, based on the pi-193 variant plus claude-184's fence-aware distinct-reviewer counter, closing issue #32.
Task size: **medium**

Skills I will invoke, in order:
1. `agentic-apps-workflow` — routing; this is a Medium change to product code in an AgenticApps repo.
2. OpenSpec **propose** (Stage 1) — the gate is core spec §18's reference implementation; changing its exit-code behavior is a spec-touching change, so it opens a change with `proposal.md` + `design.md` + spec delta + `tasks.md`. `design.md` records the two alternatives already measured (opencode-164 wholesale vs pi-193 + claude counter) and why the first was rejected.
3. `openspec validate --all` (Stage 2) — must be green before code.
4. `gate/run-plan-review.sh <slug>` (Stage 2) — ≥2 independent other-vendor reviewers into `REVIEWS.md` before any code. **Flagging now:** this is the gate that will actually bite, and it's the one the change is about — I'll surface the recursion to you rather than route around it.
5. `superpowers:test-driven-development` — the §18 truth table is a literal test spec. RED first: a `gate/run-tests.sh` asserting all seven §18 rows plus the `--pre-commit` / `--ci` rows, failing against the current 138-line copy. Then GREEN.
6. `superpowers:verification-before-completion` — before any completion claim.
7. `superpowers:requesting-code-review` — Stage 2 code review, independent context; `openspec validate` does not discharge it.
8. `superpowers:finishing-a-development-branch` — branch + PR to `main`, per the never-commit-to-main rule.

Post-phase gates (if applicable): `/review`. **Not** `/cso` — the gate is an enforcement control but touches no auth, storage, request handling, secrets, or LLM trust boundary; §14's trigger cannot occur here. **Not** `/qa` — no dev server, no user-visible surface.

Verification evidence I will produce: `gate/run-tests.sh` output showing RED against the 138-line copy and GREEN against the landed one; a measured truth-table row-by-row for all four family variants under the issue's own fixture; `--pre-commit` and `--ci` exit codes proving the modes survive; `git log` showing paired `test(RED):` / `feat(GREEN):` commits; `openspec validate --all` green.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

---

Now checking the repo state before proposing.

Repo state changes the picture materially, so I'm stopping at the decision point I committed to surface rather than routing around it.

Strong RED, plus one harness defect of my own that I need to fix before trusting section B.

Harness is committed as the RED step. Before I write the fix, one structural decision that the repo layout forced into the open.

Building the reference implementation now: pi-193 base, claude-184's counter grafted in.

Now writing the canonical implementation.

Harness bug — relative gate path breaks after the fixture `cd`. Fixing.

16/16. Adding rows for the self-review exclusion, which the harness didn't yet cover.

Verifying the per-copy numbers I asserted in that commit message.

One number in that commit message is wrong — amending.

Existing `[Unreleased]` already scopes to `tools/` + `reference-implementations/` as non-normative — so I'll keep §18's normative text untouched and let the pointer live in the README. Correcting the ADR to match.

Now the README pointer and CHANGELOG entry.

Branch is up with the fix landed. Summary of what's on `fix/32-canonical-change-gate`:

**Three commits, TDD order:**

1. `test(RED)` — `tools/change-gate-conformance.sh`. §18 says the gate must be "demonstrable by direct script invocation with simulated payloads"; nothing demonstrated it, which is *why* five copies drifted unmeasured. Stubs `openspec` on PATH so `validate` can be driven green or red independently of the gate's own logic. RED across the family: 74 passed, 19 failed, no copy conformant.
2. `feat(GREEN)` — `reference-implementations/openspec-change-gate/`. 19/19.
3. `docs` — ADR-0022, README pointer, CHANGELOG under the existing `[Unreleased]`.

**Measured scores** (same harness, all copies):

| Copy | Score |
|---|---|
| `reference-implementations/openspec-change-gate/` | **19/19** |
| `claude-workflow/bin/` · `pi-.../bin/` | 17/19 |
| `codex-` · `opencode-workflow/bin/` · `~/.agenticapps/bin/` | 14/19 |
| `core/gate/` (untracked scratch) | 11/19 + 3 inconclusive |

Two things the harness turned up that changed the plan:

**The issue's suggested fix would have regressed core.** opencode/codex-164 has no mode dispatch — `--pre-commit` and `--ci` fall through to hook mode, read empty stdin, parse no path, and fail open. Both enforcement floors become silent no-ops returning 0. Measured, section C. Since §18 makes the shell script the real enforcement surface "including against a human editor," adopting it would have traded three wrong exit codes for the loss of the only surfaces that actually guarantee the rule.

**One of my own harness rows was scoring false passes.** Section B drives a code edit under an unsatisfied change and expects a block — but a gate that fails *closed* blocks whether or not it parsed the shape, so core-138 "passed" the pi row without a working parser. Those rows now report inconclusive when the fail-open rows fail. Also fixed a relative-path bug that scored the new file 127 on every row before it scored 19/19.

Reviewer counting now requires three properties `grep -c` has none of: fenced blocks skipped, names deduplicated, self-review excluded via `OPENSPEC_GATE_SELF`.

**Two deviations from my stated plan, both flagged rather than silent:**

- **No OpenSpec change was opened.** I committed to Stage 1–2 before code. It isn't possible here: `openspec/` in this repo is a cParX dry-run migration corpus explicitly labelled "Not the live repo," not core's spec slot. Core's normative surface is `spec/*.md` + `adrs/`. Opening a change would have injected workflow-core artifacts into a simulation of another product's history. ADR-0022 is the decision record instead.
- **Stage-2 independent code review is not discharged.** It needs a separate context, and this session is configured not to spawn agents unless you ask. `openspec validate` wouldn't discharge it either. It's the one gate still outstanding.

I also left `spec/18` untouched — vendoring is offered, not mandated, and making it a MUST would be a breaking change to a 1.0.0 contract. That keeps the existing `[Unreleased]` framing ("no spec version change; `tools/` and `reference-implementations/` are not normative") true.

Verification: shellcheck 0.11.0 clean at `-S warning` on all three scripts; existing `tools/drift-report.test.sh` still 18/0; harness still scores the old copy 11/19, so it hasn't been loosened into agreeing with everything.

Nothing pushed. Want me to push and open the PR, and should I file the four host re-vendoring issues?

Every failing row across every host is `got 0` — an allow. Worth leading the adoption issue with that.

knowledge-capture: skipped (no `.planning/config.json` in this repo). Writing the handoff.

Done. Everything is up.

**PR #33** — https://github.com/agenticapps-eu/agenticapps-workflow-core/pull/33
Three commits in TDD order. Body carries the full rationale, the score table, what it deliberately doesn't do, and reviewer notes.

**Issue #34** — https://github.com/agenticapps-eu/agenticapps-workflow-core/issues/34
The adoption issue you asked for, with per-host failing-row lists so each host can go straight to its own PR once #33 lands. Highlights for them:

- **claude-workflow (17/19)** — pi payload shape, self-review counting. Plus the installer change: `install.sh:153` writes to the shared `~/.agenticapps/bin/`, so the propagation race stays open until every installer writes the same bytes.
- **pi (17/19)** — reviewer dedup + fenced example. Strongest copy; its `OPENSPEC_BIN` / `OPENSPEC_GATE_SELF` divergences were portable and are now upstream, exactly as they claimed.
- **codex + opencode (14/19, byte-identical)** — the one worth reading twice: `--pre-commit` and `--ci` are unrecognized flags that fall through to hook mode and fail open, so **the git pre-commit hook and CI check on those two hosts are currently no-ops.**

**Comment on #32** with the findings, including two corrections to the report: claude-workflow is no longer byte-identical to core (184 lines, defect 1 already fixed on its branch), and the "artifact write → 2" row is pi-specific rather than universal — it was defect 2 manifesting, matching the report's own defect 3.

The framing that came out of the measurement and is worth carrying into the host PRs: **every failing row across the entire fleet returns `0` — allow.** There is no fail-closed defect anywhere. All of it is under-enforcement, which is precisely why none of it surfaced as friction and why it sat unnoticed. The `OPENSPEC_BIN` point in #32 was the load-bearing one — the drift went unmeasured because measuring it wasn't possible.

Session handoff written.

Two things still outstanding, both in the handoff and the PR body: **Stage-2 independent code review of #33 has not been discharged** (needs a separate context; this session is configured not to spawn agents), and the `openspec update --tools` doc fix from #32 needs its own change since that file is untracked. I'd get the review done before merging, since the hosts are keyed off that merge — and I'd point the reviewer at the harness specifically, since one of its rows was scoring false passes before I caught it.

Yes — you're right, and there's probably precedent. Let me check how the last adoption round was filed rather than guess.

Precedent is clear — adoption issues go in host repos. But there's a wrinkle: **three hosts already have open §18 gate issues, and two of them assert something my measurement contradicts.**

Confirmed — codex #26 and opencode #15 both say **"no fix needed here"** on the strength of a 4-row hook-mode test. My 19-row harness contradicts that. Correcting those first rather than filing duplicates.

Now rewiring #34 to be the umbrella rather than the primary.

You were right, and checking first turned up something better than filing four new issues: **three hosts already had open §18 gate issues, and two were telling their hosts to do nothing.**

**Existing issues, corrected in place:**

- **codex-workflow #26** and **opencode-workflow #15** — both concluded *"Your in-repo gate is conformant — no fix needed here."* That came from a 4-row hook-mode test, and every row it drove does pass. But both copies are hook-only, so `--pre-commit` and `--ci` fall through to hook mode and return `0` unconditionally — the git hook and CI check in both repos are no-ops. Verdict retracted with the measurement and the 14/19 breakdown.
- **claude-workflow #96** — stale the other way. It calls the copy "the defective core copy," but that was fixed on `feat/bind-openspec-v1` (138 → 184 lines) and it now has the best reviewer counter in the fleet, 17/19. Its live half is `install.sh:153` and the shared-path race, which #33 doesn't close.

**New:**

- **pi-agentic-apps-workflow #11** — the only host without one (the work landed as PR #10 instead). Credits its four `[PI-DIVERGENCE]` fixes as correct and now upstream — `OPENSPEC_BIN` especially, since it's what made the harness possible at all — and scopes what remains to reviewer counting, including that its `OPENSPEC_GATE_SELF` branch returns early with its own `grep -vc` and so carries the same two defects plus no dedup.

**Core #34** retitled to "Fleet tracker" and rewired to point at the four host issues, keeping the comparison table for fleet-wide view.

Filing fresh issues would have left #26 and #15 sitting there contradicting them. Handoff updated.

Invoking the review skill — independence is the whole point of this gate, so it runs in a separate context.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/requesting-code-review

# Requesting Code Review

Dispatch a code reviewer subagent to catch issues before they cascade. The reviewer gets precisely crafted context for evaluation — never your session's history.

**Core principle:** Review early, review often.
