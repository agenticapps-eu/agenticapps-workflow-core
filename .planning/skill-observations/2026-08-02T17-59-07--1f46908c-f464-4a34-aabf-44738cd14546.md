## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Execute the open OpenSpec change `shim-project-hooks` (Stage 3), which has 112 tasks and 0 done; PR #60 (the plan reconciliation) is already merged to main.
Task size: large

Skills I will invoke, in order:
1. `openspec validate --all` + the §18 change-gate — Stage 2 must be re-confirmed green on current main before any code edit.
2. `opsx:apply` — the canonical Stage 3 entry point for implementing a change's tasks.
3. `superpowers:test-driven-development` — for every task marked `tdd="true"`; RED commit then GREEN commit.
4. `superpowers:systematic-debugging` — if any mid-change bug appears (Observe → Hypothesize → Test → Conclude).
5. `superpowers:verification-before-completion` — before claiming any task or the batch complete.
6. `superpowers:requesting-code-review` — Stage 2 independent code review after tasks, in a separate context.
7. `superpowers:finishing-a-development-branch` — Stage 5, compose the PR (archive is a separate act from ship).

Post-phase gates (if applicable): `/review` before landing. `/cso` is **not** triggered — this change touches hook binding and shell scripts, not auth/storage/request-handling/secrets/LLM trust boundaries; I will re-check that claim against the actual task list and revise out loud if wrong.
Verification evidence I will produce: `openspec validate --all` output, `openspec-change-gate.sh --ci` output, RED/GREEN commit pairs per TDD task, per-task grep/test evidence, and a `tasks.md` with the executed tasks checked off.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

Now let me establish the actual state.

Validate is green (5/5). Let me read the change.

Now the task list.

Change is Stage 3, 112 tasks across 9 repos. Let me survey the ground before touching anything.

Now Section 1 — diff the variants. Let me gather the ground truth.

Important finding — let me verify it precisely before recording.

All ground truth gathered. Now Section 1 — the canonical implementations.

Task 1.8 — audit both canonical implementations for other dead-precondition checks.

Now the README carrying the shim contract and the reconciliation record (tasks 1.1, 1.3a, 1.7, 1.8, 1.9).

Tasks 1.5 and 1.6 — verify empirically.

Test fixture bug — unescaped quotes in the JSON. Let me build the payloads properly.

Section 1 is green. Now correcting the artifact where execution contradicted it.

Now check off Section 1 in `tasks.md`.

Green. Committing Section 1.

Section 1 committed and the gate is green. Now Section 2 — the shims, which is TDD. Task 2.10 gates the rest: I must verify the warning channel per event class *before* writing them.

The summary conflates two rules. Task 2.10 exists precisely to not assume — let me disambiguate.

Docs verified and the two rules are now separated. Recording the finding, then writing the shim tests **first**.

RED first — the template does not exist yet.

RED observed (13 failures). But several tests pass vacuously with no template — let me fix the harness so it can't, then implement.

Now fix the harness so it can't pass vacuously, and drop the extra env var in favour of `$HOME`.

`local a=… b=$a` expands before assigning — `make_shim` was returning empty all along.

GREEN 21/21. Let me commit this as a proper RED→GREEN pair, re-verifying RED against the final test file.

Now record the task 2.10 verification in the README before committing GREEN.

Now the hour-boundary flake in the rate-limit assertion.

28 of 112 tasks done. Continuing with the conformance tooling (2.7c–2.9c) — tests first.

Now the scanner (GREEN).

Two fixes: the tool must not contain the phrase the test forbids, and the scan misses the gate shim's *legacy* override names.

The recursive scan descends into `node_modules`. Let me kill it and bound the search.

Real hits — but are they *exports* or just mentions? Let me look before tuning anything.

Not overrides at all — `grep -F` was matching `OPENSPEC_GATE` as a *prefix* of `OPENSPEC_GATE_STRICT` and `OPENSPEC_GATE_SELF`. That's a scanner bug: it must match whole variable names.

The tool reports the correct pre-rollout baseline. Committing GREEN.

Sections 1–2 complete (35/112). Now Section 3 — the installer, manifest, and provisioning states. Let me read the existing installer.

I've hit a requirement that cannot be satisfied as written. Before I improvise around a reviewed decision, two questions.

Both decisions noted. Section 3's provisioning states are defined in the design note — let me read the exact contract rather than invent one.

Design Decision 12 is authoritative — two axes, not a flat four. Writing Section 3's tests first.

Now the multi-artifact publisher.

Now the provisioning check.

Four failures, one root cause — and it's a real design flaw, not a test bug: both tools derive the expected set from what they *find*, so a missing implementation reads as a smaller-but-complete install.

Three fixture bugs — the synthetic source dirs need the declaration file too.

41/41 green. Let me run the full suite and commit.

Now tasks 3.3–3.5: publish for real on this machine and verify behaviour against the copies being replaced.

A real defect, found only by running against the real machine: the shared dir already holds three artifacts published by the *other* installer, and the check calls a healthy machine `drifted`.

Now tasks 3.4 and 3.5 — verify the published copies behave like the ones they replace.

Exactly the expected delta: identical to `callbot` except the dropped `migrations/` clause, and vs `dashboard` it now catches `.env.vercel` (task 3.5) and `MultiEdit`. Recording 3.6a/3.6b, then committing.

All core-side work is done (60/112). The deletions in section 4 are gated by section 5.0's justification checks — those must run first. Starting with the transitive-consumer search (5.0c, 5.0c-i).

Two important gaps. Let me tighten the log-reader search (5.0c-i demands a citation, not an inference).

`skill-observations` turns up an entire dashboard product surface — including a `HookFirings` panel. I need to know whether it reads the files the deleted hook writes.

`readSkillObservations` globs `*.jsonl` — so it *does* read `skill-router-*.jsonl`. Whether those records survive parsing decides the finding.

Decisive. Let me confirm the last piece for 5.0b before writing the record.

Now the deletion record — the three clauses, per hook, against every applicable spec.

All core-side work is done and the deletions are now justified on record. Writing the handoff before the multi-repo rollout.

Implemented `shim-project-hooks` from 0 → 67 of 114 tasks on `feat/shim-project-hooks` (9 commits, unpushed). **All core-side work is done and verified; what remains is the multi-repo rollout.**
