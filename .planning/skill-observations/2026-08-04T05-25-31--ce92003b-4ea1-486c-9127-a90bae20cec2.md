## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Move the three-axis provisioning state model, all currency invariants and the six currency scenarios out of the requirement "An unresolvable shim allows, and the operator sees it" and into "Provisioning is checked per machine, not only per repository", so the durable capability's requirements are named by what they actually govern.
Task size: **medium**

Skills I will invoke, in order:
1. `superpowers:brainstorming` — this is a restructuring of durable capability text with more than one defensible cut line; alternatives must be written down, not merely considered.
2. `/opsx:propose` — Medium work opens an OpenSpec change; produces proposal + design note + spec delta + tasks.
3. `openspec validate --all` — Stage 2, enforced, before any edit to the spec body.
4. `run-plan-review.sh <slug>` — Stage 2, ≥2 independent other-vendor reviewers, before code. Not enforced since gate 2.0.0, which is exactly why I am stating it here.
5. `superpowers:verification-before-completion` — before claiming the move is complete.
6. `superpowers:requesting-code-review` — Stage 2 independent review; `openspec validate` does not discharge it.
7. `/opsx:archive` then `superpowers:finishing-a-development-branch` — archive and ship are two separate acts.

Post-phase gates (if applicable): `/review` at ship time. **No `/cso`** — this change touches spec prose only, with no auth, storage, request handling, secrets, or LLM trust boundary. **No `/qa`, no `/design-shotgun`, no `database-sentinel`** — no UI surface, no schema, no user-visible runtime behavior.

Verification evidence I will produce: `openspec validate --all` green; `REVIEWS.md` with ≥2 reviewers; a before/after requirement-to-line-count map proving no normative sentence was lost in the move; `git diff --stat` showing the move is content-preserving; project-hook suites still 243/243 green; gate `--ci` clean.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

One thing I want to flag before I start, because it shapes the proposal: this is a **pure relocation of normative text**, and the main risk is not breaking a tool — it is silently dropping or subtly reweighting a sentence during a ~200-line move. That risk is exactly the "an error rendered as a passing observation" failure this repo has now hit three sessions running. So my verification will be a mechanical sentence-level diff, not a reading.

Starting with brainstorming, since the cut line genuinely has alternatives.

The target requirement already says *"computed observationally per the state table above"* — it depends on a state table it doesn't own. That's the structural tell. Let me locate the state model's exact boundaries.

Structure is clear enough to brainstorm against: prose state model ~611–800, remaining prose to 960, then 16 scenarios (961–1085) of which the last 11 are provisioning/currency. Invoking brainstorming as committed.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
