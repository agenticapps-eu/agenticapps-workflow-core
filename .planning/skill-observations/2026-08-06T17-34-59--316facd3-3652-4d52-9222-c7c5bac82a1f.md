## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Propose an OpenSpec change that removes host hook wiring, moves the enforcement floor to a global `core.hooksPath`, and drops the deferred `--project` scope.
Task size: large

Skills I will invoke, in order:
1. `superpowers:brainstorming` — this is an architecture decision with real alternatives; ≥2 must be recorded in `design.md`
2. `/opsx:propose` — author proposal + design + spec delta + tasks
3. `openspec validate --all` — enforced, before any code
4. `openspec-change-review` / `run-plan-review.sh` — step 2b, other-vendor reviewers before code exists

Post-phase gates: plan-review. No code is written this turn.
Verification evidence I will produce: the change directory artifacts, a green `openspec validate --all`, and a recorded answer to the `core.hooksPath` displacement risk below.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

Two notes before I start. The skill that loaded is the **402-line copy from the archived `claude-workflow` checkout**, not core's v4.0.0 — the exact duplicate-trigger-skill condition the current change exists to fix. Where they conflict I follow core's version (e.g. its red flag 14 asserting the gate blocks under 2 reviewers is stale). And `core.hooksPath` *replaces* the hooks directory rather than adding to it, so before proposing anything I need to know what it would displace.

All nine git hooks on this machine are the workflow's own gate — no husky, no lefthook, nothing else. So a global `core.hooksPath` displaces nothing. It also surfaced something that argues *for* the change:

All nine hooks are four *divergent* copies of the same gate (883, 1201, 2270 and 5844 bytes) — which is an argument for the change, not just an absence of one against it. Creating the change:

One claim in that risk table needs verifying rather than asserting — whether a repo's own `.git/hooks/pre-commit` overrides `core.hooksPath`. Cheap to test:

Backwards — `core.hooksPath` wins and `.git/hooks/` is ignored entirely. Correcting the design:

Now the spec delta and tasks.

Change is proposed and validates. Here's what came out of it that you should know.

**`openspec/changes/one-enforcement-floor/`** — proposal, design, spec delta against `workflow-installation`, 41 tasks. `openspec validate --all` is 10/10 green.

**The evidence moved two of the three decisions from "reasonable" to "measured."**

The displacement risk on `core.hooksPath` was the thing that could have killed the git-hook move — it *replaces* the hooks directory rather than adding to it, so a global binding can silently disable existing per-repo hooks. Across `~/Sourcecode`, exactly nine repos have a non-sample hook, all nine are `pre-commit`, and all nine are this gate. No husky, no lefthook, no `pre-push`. The displaced set is empty.

Better than that, it argues *for* the change: those nine copies are **883, 1201, 2270 and 5844 bytes** — four versions of one authority, and nothing on the machine reports the divergence. Per-repo copying already produced the failure it was meant to prevent.

**I got one thing backwards and corrected it.** I'd written that a repo's own `.git/hooks/pre-commit` wins over `core.hooksPath`. It doesn't — I tested it in a scratch repo, and the global hook runs while the local one is ignored entirely and silently. That makes the nine stale copies *inert* rather than competing, which is safer, but the change still removes them: an executable hook on disk that never runs is read by the next person as the one that does.

**The sequencing finding is the one that affects what you do next.** `workflow-installation` doesn't exist in `openspec/specs/` — it's created by `core-installer-one-entry-point`, still in flight. A `REMOVED`/`MODIFIED` delta against an unarchived requirement has nothing to modify, so this change is blocked until that one archives.

The practical consequence: **the real run should skip `--accept-host-config`.** Wiring three hosts' config files in a run whose successor deletes that wiring is work done twice, on files your editors read, with the parallel-session race attached. Without the flag the wiring reports as skipped and the run exits non-zero — which is honest, and the skill rebinding is what that run is actually for.

**One thing worth noticing:** `validate` passed 10/10 on a change whose removals target a capability that isn't in `specs/` at all. That's your own rationalisation-table entry demonstrating itself — validate is a schema check and cannot tell you the delta describes something that doesn't exist. It's exactly what step 2b is for.

Design records the alternatives, including the strongest one I could build against you: keep the host hook and turn on `OPENSPEC_GATE_STRICT=1`, which would make it genuinely load-bearing. I rejected it because `pre-commit` can enforce the same rule with one implementation instead of three — but if you want "no code without a change", that's the lever, and it should be pulled at the floor.

It also names the question this change refuses to answer by implication: whether `AGENTS.md` still needs a workflow section once the skill carries the workflow. Repealing that requirement as a side effect of a hooks change is how a rule vanishes without anyone deciding. The live counter-evidence is in this session — the skill that loaded was the 402-line copy from the *archived* checkout, not core's v4.0.0.

Nothing committed: the doc and the change are both sitting in the working tree. Next step in the loop is 2b, the other-vendor reviewers — want me to run them, or do you want to read the proposal first?

You're right, and there's a cleaner version of your instinct than the one I proposed.

My "skip `--accept-host-config`" answer avoids *running* the wiring but still ships it: 27 installer lines plus all 293 lines of `hosts/` land in main, dead on arrival, and get deleted one change later. That's not much better than installing and uninstalling — it's just quieter.

The better move is to **strip the wiring out of `core-installer-one-entry-point` before it lands at all.**

That narrows the in-flight change to what it's actually about — one installer that publishes the payload and binds skills — and it has four consequences that all point the same way:

- The real run becomes `./install.sh --host auto --replace-unrecognised`. It touches no file your editors read, so the parallel-session race we spent this morning on disappears entirely.
- `install.sh` drops roughly 33 executable lines (the wiring block, `accept_host_config`, the two path vars, the flag parsing), landing near 217. That fixes the at-the-ceiling problem — right now it's pinned at exactly 250 with no headroom.
- `hosts/` is never published, so Phase 5b's remaining blocker is gone before Phase 5b starts rather than after.
- `one-enforcement-floor` stops needing a `REMOVED Requirements` section, because the wiring requirements were never archived. Its delta gets simpler.

Narrowing is also consistent with this change's own history rather than against it: two review rounds already forced `--project` out of it for the same reason — a differently-shaped concern bundled into a change whose end state is different. Doing the same thing again to the wiring is the precedent, not an exception to it.

What it costs, honestly: the change is at 63/69 and twice-reviewed, and reopening scope invalidates the round-three evidence — though that digest is already stale from this morning's spec edit. The `wire_opencode` function and both host artifacts were carried forward and corrected this week, and that work gets deleted. The red-flag table is explicit that sunk-cost reasoning about deleting code you've decided is wrong is itself the failure mode, so I don't think that's an argument to keep it.

The alternative is folding the global floor into the current change as well, so one change lands the whole end state. I'd not do that — it bundles two decisions, which is exactly what the reviewers pushed back on before, and it makes the enforcement-model change unreviewable as its own thing.

**On the design questions: yes, deferred, not dropped.** Four are queued and each has a trigger rather than a vague "later":

`AGENTS.md`'s workflow section is the big one — it needs its own change and its own evidence about whether the skill reliably loads on every host, and this session produced live counter-evidence when the archived 402-line copy loaded instead of core's v4.0.0. The `--check` archived-binding blind spot is a one-liner once the budget has headroom again, which the narrowing above provides. Moving §11 and the workflow rules out of the global `CLAUDE.md` is a prerequisite for claiming nothing host-specific remains. And whether `project-hook-binding`'s shim mechanism survives a global floor is a separate argument on separate evidence — `database-sentinel` is shimmed too, so the gate moving doesn't settle it.

The 2b reviewers will very likely raise some of these unprompted. That's them working, not a problem to pre-empt.

Want me to do the narrowing — revise the in-flight change's delta, strip the wiring from `install.sh` and its tests, delete `hosts/`, and rewrite `one-enforcement-floor`'s delta to suit? It's a scope reopen on a nearly-done change, so I'd rather you say go than assume it.

Doing it. First, mapping exactly what comes out of `install.sh`.

Now the edits to `install.sh`.

Now the wiring block itself.

210 executable lines, down from 250 — 40 out, better than my estimate. Now `hosts/` and the tests.

Now the test surgery: drop the wiring section, strip the dead flag, and repurpose the canary into the negative test for this change.

Now the `--help` test, the now-dead `jq` test, and the section banner.

`host_exec` lived inside the deleted section — and three tests passed *without* it, which is a vacuous pass. Restoring it next to the other scaffolding:

Task 1.5 runs before `bind_case` is defined — the helper has to sit higher.

46/46 clean with no silent failures. Proving the new negative tests aren't vacuous — the task 4.3 pattern against the pre-narrowing installer:

Both discriminate — 5 hits/exit 0 before, 0 hits/exit 64 after. Now the change artifacts. The spec delta first:

The delta's only remaining "wiring" mentions are in the budget's own history, which is where they belong. Now the proposal and tasks:

Now the tasks file, then `one-enforcement-floor` needs its `REMOVED` section dropped since those requirements no longer exist.

Now `one-enforcement-floor` loses its `REMOVED` section — those requirements no longer exist to remove.

The successor's budget requirement is now stale — it assumes it removes the wiring itself.

The doc's hooks section is wrong as of now, not later. Updating it:

Narrowing is done and everything's green. Staged, not committed.

**`install.sh`: 250 → 210 executable lines.** Out: `wire_json`, `wire_claude`, `wire_codex`, `wire_opencode`, `accept_host_config`, the `--accept-host-config` flag and its env var, the `ADAPTER`/`PLUGIN_SRC`/`GATE` paths, and the `jq` dependency — jq was Tier 2 for the JSON merge alone. `hosts/` is deleted. The only host names left in the file are the `HOSTS` table, the `ARCHIVED` tombstones, and `neutral_of`'s prefix-stripping, all of which are data rather than behaviour branches.

`install_hosts` now has no per-host branch at all, which is the property worth protecting: the moment one exists, the second is cheap.

**Tests: 58 → 46, and the replacements assert files rather than absent code.** Thirteen wiring cases went. Three new ones came in, because a test that only checked the `wire_*` functions were gone would pass against an installer that wrote the same files by another route:

- 4.1 — binding a host creates no config file and modifies no existing one, while the skills still bind
- 4.2 — `--accept-host-config` exits 64 with the argument named, rather than being silently ignored
- 4.3 — `install.sh` references no host-config artifact and `hosts/` does not exist

I proved they discriminate rather than trusting them: against the pre-narrowing installer, 4.3's pattern hits 5 lines and `--accept-host-config` exits 0; after, 0 hits and exit 64. The jq case was inverted too — it used to assert jq's absence was *politely reported*, and now asserts a full run never mentions it.

**One real bug caught on the way.** `host_exec` was defined inside the deleted wiring block, so three cases ran calling a function that no longer existed — and passed. That's precisely the vacuous pass this suite has a guard for. It now sits with the scaffolding and above its first caller, because a definition below its caller is the same silent pass wearing a different hat.

**The budget went to 228, not left at 250.** The raise from 200 was itemised across five items; two of them were wiring (`wire_opencode` 10, the second opt-in 12), so 22 comes back. Leaving the ceiling where the removed behaviour put it would quietly fund the next thing nobody argued for. Headroom is 18 and it's stated in the requirement rather than discovered later.

**Both changes are consistent again.** The predecessor's delta swaps three wiring requirements for one "The installer writes no host configuration". `one-enforcement-floor` loses its whole `REMOVED` section — there's nothing left to remove — and its budget requirement now says the opposite of what it did: this change *spends*, so it's explicitly not pre-raised, and if the floor binding doesn't fit in 18 lines the overage gets itemised rather than absorbed.

`openspec validate --all` 10/10, tests 46/46, `--check` byte-identical to `docs/evidence/install-check-before.md`. `docs/HOW-IT-FITS-TOGETHER.md` is updated — it went from "one gate, three surfaces" to two, and its open-questions section drops the host-hook question, which is now answered, keeping the `AGENTS.md` one.

Two things I did not do. `9.1` in the predecessor is now marked superseded rather than open, since `--project` is dropped by the successor rather than built. And I added `9.2`: **`REVIEWS.md` reviews an installer that wired three hosts** — a scope that no longer exists, so round three is stale in a way that matters more than the digest drift.

The real run is now `./install.sh --host auto --replace-unrecognised`, with no configuration opt-in to pass because there isn't one — which is what makes it safe to do beside your live sessions. Want me to commit this, and then re-run the plan reviewers against the narrowed scope before the run?

Committed. Now the reviewers against the narrowed scope:

Both commits are in on `feat/one-skills-payload`, and round four is running against the narrowed change now.

- `cc37fe5` — the narrowing: `install.sh` 250→210, `hosts/` deleted, tests 58→46 with the three that assert files rather than absent code
- `4fc06cc` — `one-enforcement-floor` plus `docs/HOW-IT-FITS-TOGETHER.md`

The gate printed its two expected NOTEs on both commits: the predecessor's review no longer describes its text, and the successor has no review yet. Both non-blocking, both accurate.

While that runs, one thing worth flagging about what the reviewers are about to see. `REVIEWS.md` currently reviews an installer that wired three hosts — that scope is gone, so this isn't a re-review of the same artifacts with changes applied, it's closer to a first review of a different change. If round four comes back with far fewer findings than round three, that's the narrowing having removed the surface most of the findings were about, not the plan getting better.

After this lands there are two open review items rather than one: this round for the predecessor, and `one-enforcement-floor`'s own 2b, which is its task 8.2 and hasn't run. That one has no code and won't for a while, which is exactly when a plan review is cheapest — but it's a separate egress, so I'll ask rather than assume.

I'll report when the reviewers finish.

Both REQUEST-CHANGES, and both lead with the same finding. It's correct — I revised the proposal, the delta and the tasks, and never touched `design.md`.

All three concrete claims verified. `design.md` has 34 wiring references and was never touched; task 1.6 still asserts 250; and the false manifest claim survives in `design.md:149` and `proposal.md:114` — I fixed only the handoff and `tasks.md` this morning.

Reconciling `design.md` — replacing the wiring decisions while keeping what's still load-bearing:

Now the budget arithmetic — gemini's point that the calculation was incomplete is correct, and the answer isn't 228.

Three reviewer flags across two vendors on the same gap — the equivalence heuristic is normatively undefined. Putting it in the requirement:

Round four came back **REQUEST-CHANGES from both**, and they were right about something I missed.

**The headline finding is mine.** Narrowing the change touched `proposal.md`, the spec delta and `tasks.md` — and never `design.md`, which still specified host wiring in 34 places. Both reviewers independently led with it. A bundle that contradicts itself is worse than one that's merely wrong, because a reader can't tell which half is live. Six decision sections are now replaced by one recording the removal and its rejected alternatives.

I kept what the deleted artifacts *knew*: why codex needs an adapter at all (`apply_patch` carries paths inside the patch body and codex expects a permission-decision response), and that the installed opencode plugin still asserts the two-review rule the gate dropped at 2.0.0. Those facts outlive the files.

**The budget was wrong, and gemini's objection was the substantive one.** I'd reversed only the two wiring items from the 200→250 raise and landed on 228. The measured removal was 40 lines — the other 18 were wiring that predated the raise and sat inside the original 200. The correct number is **217**: 200, minus 18 of baseline wiring, plus the 35 non-wiring lines of the raise. Implementation is 210, so headroom is 7. The successor now states outright that 7 won't be enough and that its overage must be itemised rather than pre-approved.

**The stale-manifest claim survived in two more places.** I corrected the handoff and `tasks.md` this morning and left `design.md:149` and `proposal.md:114` still asserting the `normalize-claude-md.sh` row disappears. Both fixed, and both now name it as a corrected claim rather than quietly reading right.

**The equivalence derivation is now normative** — gemini in round four, codex in rounds three and four. "Host-neutral equivalent" appeared in a SHALL while the rule computing it lived only in a Decisions section. It's specified now, with two new scenarios for the undefined cases, and with a plain statement of what it doesn't guarantee. Three flags across two vendors is signal: `codex-impeccable-audit → impeccable` is a name transformation, not a capability comparison. I accepted the objection on three stated bounds rather than waving it off, and recorded that an explicit reviewed mapping is the right answer if a mis-rebind is ever actually observed.

`87d1448`. Validate 10/10, tests 46/46, installer at 210 against 217.

Two things I did **not** do, both deliberate. Codex's finding that the delta omits the authoritative host/directory/marker tables is a real spec-precision gap, but it's the same objection from round three and it argues for enumerating data that already lives in one place in the code — I'd want your call before inflating the delta with tables that then need syncing. And its finding about escaping control characters in reported paths is legitimate but is a different change: the paths are operator-supplied, not hostile input, and the fix belongs with `screen-review-egress` where the PII policy already lives.

The narrowed change is now internally consistent for the first time since the scope moved. Next is the real run — `./install.sh --host auto --replace-unrecognised`, no configuration opt-in because there isn't one — and `one-enforcement-floor` still has no plan review of its own.

Handoff written and committed (`6405774`). Working tree is clean apart from the untracked `.planning/`.

Six commits this session, all on `feat/one-skills-payload`:

`b707a5f` round three · `1bbe826` the two fixes it produced · `cc37fe5` the narrowing · `4fc06cc` the successor change and the topology doc · `87d1448` round four · `6405774` the handoff

I also saved a memory — **narrow in-flight rather than ship-then-delete** — since that was your call today and it generalises past this change, along with the two things it cost me: reconcile *every* artifact rather than the ones you remember, and lower the budget when scope leaves instead of keeping the room the removal freed.

The handoff's first line points at the run, and it's a smaller run than the one this morning's handoff described. No config opt-in exists to pass, so the parallel-session hazard we spent the morning on went with the wiring:

```
./install.sh --host auto --replace-unrecognised
```

One thing worth knowing before you clear, because it affects this session specifically: the trigger skill currently loading is the 402-line copy from the **archived** `claude-workflow` checkout, not core's v4.0.0. The run fixes that — which means after the run, a fresh session gets a different (correct) workflow skill than this one had.

Clear when ready and I'll pick up from the handoff.
