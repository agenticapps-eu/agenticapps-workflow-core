## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: The shim's unresolved-implementation report is rate-limited but its `exit 1` is not, so every call after the first in an hour surfaces as a contentless `hook error — No stderr output`.
Task size: medium

Skills I will invoke, in order:
1. `superpowers:brainstorming` — the fix has ≥2 real alternatives (suppress the exit code with the message vs. always emit a short line), and picking wrong changes what the shim contract promises.
2. `/opsx:propose` — the shim contract's reporting behaviour is specified in `openspec/specs/project-hook-binding`, so this needs a spec delta, not an edit.
3. `openspec validate --all` + `run-plan-review.sh` — Stage 2, before any code.
4. `superpowers:test-driven-development` — `tools/project-hook-shim.test.sh` already asserts shim exit codes; the new behaviour goes in RED first.
5. `superpowers:verification-before-completion` — evidence before any completion claim.
6. `superpowers:requesting-code-review` (Stage 2, independent context) → `superpowers:finishing-a-development-branch`.

Post-phase gates: none of `cso`/`qa`/`database-sentinel` apply — this touches a hook shim's reporting path, no auth, storage, schema or UI surface.
Verification evidence I will produce: RED/GREEN commit pair on `tools/project-hook-shim.test.sh`, live three-consecutive-call reproduction showing the new behaviour, `openspec validate --all` green, gate `--ci` exit 0.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

Let me read the governing requirement before brainstorming, so the options are grounded in what the spec actually promises.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
