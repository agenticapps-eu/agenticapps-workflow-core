## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Install core's own §18 change gate — PreToolUse hook shim, settings registration, git pre-commit wrapper, and the CI floor — in agenticapps-workflow-core, the repo that publishes the gate but runs none of it.
Task size: medium

Skills I will invoke, in order:
1. `superpowers:brainstorming` — the shape isn't settled: core self-gating has a real bootstrapping problem (a gate that blocks edits while `shim-project-hooks` is open would block its own installation), and ≥2 alternatives belong in `design.md`.
2. `/opsx:propose` — Medium work opens an OpenSpec change; this writes a MODIFIED delta against the `change-gate-enforcement` capability that #54 just promoted to `openspec/specs/`.
3. `openspec validate --all` — Stage 2, enforced, before any code.
4. `run-plan-review.sh <slug>` — Stage 2 review; needs vendor CLI egress, so I will prepare it and hand the trigger to you rather than claim it ran.
5. `superpowers:test-driven-development` — the gate's behaviour in core is testable by direct invocation with simulated PreToolUse payloads; RED before GREEN.
6. `superpowers:verification-before-completion` — before any completion claim.
7. `superpowers:requesting-code-review` — Stage 2 code review in an independent context; `openspec validate` does not discharge it.

Post-phase gates (if applicable): review
Verification evidence I will produce: `openspec validate --all` output; direct gate invocation with simulated payloads showing exit 2 / exit 0; `openspec-change-gate.sh --ci` run inside core; the CI job green on the PR.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

---

First, checking whether this is already in scope of the open change rather than opening an overlapping one.

No overlap — `shim-project-hooks` treats core's lack of hooks as a fact, not something it fixes. Core self-gating is genuinely separate. Reading the design constraints before brainstorming.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
