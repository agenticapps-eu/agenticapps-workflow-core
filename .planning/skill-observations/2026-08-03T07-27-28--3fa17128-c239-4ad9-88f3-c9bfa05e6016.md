## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Discharge task 7.6 of the open `shim-project-hooks` change — fix finding 6 (an override naming a directory is `exec`'d instead of reported), bump `shim-contract` 1.0.0 → 1.1.0, re-render and propagate the shim into eight files across seven repositories, and record the empty-override behaviour as intended.
Task size: **large** (fleet propagation across seven repos, eight PRs)

Skills I will invoke, in order:
1. `superpowers:test-driven-development` — 7.6 is a behaviour change in the shim template with an existing 155-assertion suite; RED first on the directory-override case, then the one-line fix.
2. `superpowers:verification-before-completion` — before claiming any task or PR done; evidence from the five suites, `openspec validate --all`, gate `--ci`, gate conformance, and the fleet-wide `IDENTITY` check.
3. `superpowers:requesting-code-review` — the change already carries its Stage-2 review; this is a §7 remediation commit against it, reviewed on the PR.
4. `superpowers:finishing-a-development-branch` — to compose the core PR and the seven propagation PRs.
5. `/opsx:archive` — only once 7.6 is checked and the fleet is green.

No new OpenSpec change: this work is task 7.6 of an already-proposed, already-validated change, and it changes no requirement — the delta already says "executable **regular file**"; the code is what's non-conforming.

Post-phase gates: `/review` on the core diff before the PR.
Verification evidence I will produce: RED output for the directory-override assertion, suite counts before/after, `openspec validate --all`, `openspec-change-gate.sh --ci`, `project-hook-conformance.sh` per repo, and the `IDENTITY` check green across all eight shims at contract 1.1.0.

Now let me read the actual machinery before touching anything.

20 shim files across the 7 repos, plus core's self-hosting binder — 21 binders, not the 8 the handoff carried. (The spec's "eight files" counts `openspec-change-gate` binders only; the template also renders into `database-sentinel` and `normalize-claude-md` shims, which this fix touches too.) Still 7 fleet PRs + core, but the diff is wider than advertised. Proceeding.

Baseline confirmed: 155/155 green across the five suites. Now the TDD cycle.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/test-driven-development

# Test-Driven Development (TDD)
