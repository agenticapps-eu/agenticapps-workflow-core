## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Port ADR-0047 to `codex-workflow`, `opencode-workflow` and `pi-agentic-apps-workflow` — each installer resolves core's artifacts from a pinned manifest instead of publishing stale vendored copies (all three ship gate 1.3.1 / wrapper 1.1.0).
Task size: large (three repos, each with its own installer, tests and CI)

Skills I will invoke, in order:
1. `superpowers:test-driven-development` — a failing check per repo before wiring, mirroring `test_core_artifacts_resolve_from_pin`.
2. `superpowers:verification-before-completion` — each repo's own suite plus a real installer run, evidence captured.
3. `superpowers:requesting-code-review` — flagged: I'll self-review, since this session bars subagents unless you ask.

Post-phase gates: each repo's CI must go green before merge; no UI or auth surface, so no `/cso` or `/qa`.
Verification evidence I will produce: per-repo before/after test counts, resolved-vs-pin byte comparison, installer dry-run output, CI status.

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

Surveying all three before touching anything.

All three are clean and structurally uniform. Let me look at how each installs, and what references the files I'd delete.

`opencode`'s conformance harness scans **sibling hosts'** vendored copies — including claude-workflow's, which I just deleted. Let me check whether I've already broken it.

`--family` mode filters missing paths with `[ -f ]`, so claude-workflow's deletion silently drops a target rather than failing — no breakage, but the sweep now scores one fewer host and still reports success. Same silent-coverage class; I'll note it.

Let me set up tracking and start with codex.

Codex's shape is clean — two `install-gate.sh` calls whose SRC I redirect. Let me check what reads its manifest before rewriting it.

Before writing any manifest I need to know whether each host's conformance harnesses are actually core's bytes or host-specific forks — I can't pin a fork.

All three carry the **same** older harnesses (547/215 lines) — so they're stale copies, not host-specific forks. Let me confirm that, because it decides whether I can simply re-vendor them.

All three carry core's harnesses at commit `636f106`, byte-identical and unforked — just stale. Re-vendoring is legitimate. Baseline for codex first.

Noted — I'll run the full commit → PR → merge cycle myself from here without pausing, still via branches and PRs.

Codex baseline: **562 pass, 1 skip, 0 fail**. Now wiring it.

Now wiring codex's `install.sh` to resolve before publishing.

Now delete the vendored copies and update codex's CI and tests.

Now codex's CI workflow and its test suite.

Now codex's test suite. Let me see exactly what breaks.

17 failures, all from tests reading the two deleted paths. Let me enumerate the exact sites rather than blanket-replacing.

A **third** copy I hadn't found — the setup skill's `templates/` ships the gate and wrapper for target repos.

The templates are **symlinks into `bin/`**, now dangling — and codex has many internal consumers of `bin/`. Deleting the bytes (what I did for claude) is the wrong shape here. Better model for codex: **`bin/` becomes a gitignored cache materialised from the pin** — nothing is committed, nothing can drift, and every existing consumer keeps working.

Now simplify `install.sh` and `openspec-gate-ci.sh` to use the materialiser, and gitignore the cached bytes.

Now materialise and run codex's suite — the tests should pass unchanged, since the files are back on disk and match the manifest.

Down from 17 to 8, and all eight are one class: tests asserting the gate **blocks on unreviewed changes**. That's the pre-2.0.0 contract — this is the real consequence of moving codex from gate 1.3.1 to 2.0.0. Let me look at them.

Now updating the truth table to the 2.0.0 contract.

Now the remaining six, which all drive the deny path through an *unreviewed* change. Let me see how each builds its fixture.

Codex: **563 pass, 0 fail** (baseline 562 — the extra row is the new "advisory is not silent" assertion). Verifying the CI path end to end.

You're right that this has taken too long, and the cause is mine: I opened codex's PR before checking that repo's *other* two CI workflows, so three jobs are red on my change. Merging it red would be worse. Let me get the reason in one shot and then write the handoff.

Root cause is simple and my fault: `ci.yml` runs the harness without materialising first — I only did that by hand locally. Fixing it in `run-tests.sh` so every caller is covered, not just CI.

No — there's no incentive to burn tokens, and I'm not padding. The honest cause is a planning error: I should have surveyed all three hosts' installers, tests and CI **before** starting, instead of discovering codex's symlinked templates, its bespoke test suite and its second CI workflow one failure at a time. That's on me, not on the task being hard.

Writing the handoff now.

Handoff is written and pushed. Codex went green and is merged.

**Merged this session:** core #47 (`6cd3b9c`), claude-workflow #109 (`70ef922`), codex-workflow #33 (`6c9403a`). All three hosts' installers that I touched now publish core's artifacts from a hash-verified pin. **Not started:** opencode and pi.

On your criticism — the slowness was real and the cause was mine. I started codex before reading its installer, its 560-row test suite, and its second CI workflow, so I hit three separate surprises one at a time: templates that are symlinks into `bin/`, eight tests encoding the pre-2.0.0 "reviews block" contract, and `ci.yml` running the harness on a clean clone. Each cost a round trip that a ten-minute survey would have collapsed into one pass. I've written that lesson at the top of the handoff as the first instruction for whoever continues, because opencode and pi will hit the same class of thing and pi is the more divergent of the two.

I won't argue the comparison with other harnesses — that's yours to judge on the output. What I'd say factually is that the work that landed is verified rather than assumed: real installer runs, byte comparisons against the pin, mutation-tested assertions, and two of my own defects caught before merge (a temp-file leak and a vacuous test row).

If you want to keep going, the fastest path for opencode and pi is the recipe in the handoff — and this time I'd survey both repos completely before writing anything, then do them in one pass each.
