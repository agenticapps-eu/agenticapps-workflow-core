## Workflow commitment

I am using the agentic-apps-workflow skill for this task.
Task scope: Resolve the axes-table Currency contradiction in `openspec/specs/project-hook-binding/spec.md` — the table says an artifact the authority lacks is `stale`, while the currency requirement says an artifact absent from the machine is not judged on that axis at all.
Task size: medium (normative spec change → opens an OpenSpec change)

Skills I will invoke, in order:
1. `agentic-apps-workflow` — routes this to the OpenSpec lifecycle (already invoked)
2. `superpowers:brainstorming` — the contradiction has ≥2 defensible resolutions (fix the table vs. fix the requirement); alternatives must be recorded in `design.md`
3. `/opsx:propose` — author proposal + design note + spec delta + tasks
4. `openspec validate --all` — Stage 2, enforced, before any edit to the spec slot
5. `run-plan-review.sh <slug>` — Stage 2 multi-AI change review, ≥2 independent reviewers, before code
6. `/opsx:apply` — implement under the retained gates
7. `superpowers:verification-before-completion` — evidence before any completion claim
8. `superpowers:requesting-code-review` — Stage 2 independent review in a cleared session
9. `/opsx:archive` then `superpowers:finishing-a-development-branch` — archive and ship as two separate acts

Post-phase gates: none of `qa` / `cso` apply — this is a documentation-normative change with no code, no UI, no trust boundary.
Verification evidence I will produce: the two contradicting passages quoted verbatim with line numbers; `openspec validate --all` green; `REVIEWS.md` reviewer count; the folded spec text showing one consistent rule.

First, verifying the defect is real and still present.

Both defects confirmed present. And reading the surrounding text, I found a **second, worse one**. Let me check whether it predates the split.

Confirmed: the tear was introduced by the **archive fold at `09f829e`**, not by the split. Original delta had it as one intact paragraph. Let me check which side of the contradiction the implementation actually takes, and read the related scenarios.

Both fragments and the table sit in just two requirements — and the tooling constraints from last session don't bite (no scenario moves, no ADDED/REMOVED). Here's what I found before writing anything:

**Defect B — the one the handoff predicted (real, normative).** The axes table scopes `current` to *present* artifacts but forgets that qualifier in its `stale` clause:
- `spec.md:847` — "`stale` when any differs, **or the authority holds no file for a declared artifact**"
- `spec.md:989` — "Currency is judged over the declared artifacts that are **PRESENT**. An artifact that is absent from the machine … SHALL NOT also be reported by this axis, **whether or not the authority holds it**"

It's three-against-one, not an open question. The requirement prose, the scenario at `spec.md:1076`, *and* the implementation all agree — `tools/provisioning-check.sh:422` reads `[ -f "$art" ] || continue`, skipping absent artifacts outright. The table cell is the sole outlier; it lost a qualifier.

**Defect A — new, and worse.** A three-line fragment is torn out of its paragraph and filed under the wrong requirement, ~66 lines away:

> `spec.md:861` (ends mid-sentence) … `DELETE FROM public.users` was
> `spec.md:927` (opens a different requirement) **not** blocked. Both are defects the fleet believed were fixed…

Intact in the delta at `db02493`, severed by the **archive fold at `09f829e`** — not by the split PR #70. This is a sixth instance of the handoff's theme, and the sharpest: the line-multiset diff proved every line survived, and every line *did* survive — in the wrong requirement. Two requirements currently each contain half a sentence.

Both are pre-existing in `main`. Both live in **"A machine's provisioning is a triple"**; Defect A also touches **"Currency is judged against an authority checkout"**.

Scoping settled: one change, both defects, each labeled distinctly in the delta. Per my commitment, brainstorming next — the alternatives need to be written down in `design.md`, not just considered.

Baseline green with both defects present — which is itself the point: `openspec validate` passes a spec containing two half-sentences and a self-contradicting table. Schema validation cannot see either.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
